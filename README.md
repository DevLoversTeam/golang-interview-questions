**Read in other languages: [English 🇺🇸](README.en.md),
[Polska 🇵🇱](README.pl.md), [German 🇩🇪](README.de.md), [French 🇫🇷](README.fr.md),
[Spanish 🇪🇸](README.es.md), [Українська 🇺🇦](README.md).**

<h1>
  Go <img src="./assets/go.svg" width="40" height="40" />
</h1>

<h2>Найпопулярніші запитання та відповіді на співбесіді з Go</h2>

---

## 1. Основи мови

1. Що таке Go і для яких задач його було створено?
2. Які основні принципи дизайну мови Go?
3. Які ключові особливості Go порівняно з іншими мовами?
4. У чому різниця між імперативною і декларативною парадигмою програмування?
   Наведіть приклади мов.
5. Чому Go добре підходить для написання Cloud Native сервісів?

---

## 2. Типи даних, слайси та мапи

1. Що таке `shadowing` змінних і як він може призвести до помилок у
   бізнес-логіці?
2. Навіщо використовувати `struct{}` (порожню структуру) і в яких сценаріях це
   ефективно?
3. Як працює внутрішня структура `slice` і що відбувається при передачі його до
   функції?
4. Чому `make([]T, 0, n)` краще ніж `var s []T` при відомих розмірах?
5. Як slice expression `a[low:high:max]` контролює `cap` нового слайса?
6. Чи гарантується, що вказівник на елемент слайсу залишиться валідним після
   виклику `append`?
7. Як у Go ефективно видаляти елементи зі слайса без збереження порядку?
8. Який порядок ітерації ключів у `map` і чи можна на нього покладатися? Як це
   впливає на тести та серіалізацію?
9. Як ітерувати `map` у передбачуваному порядку?
10. Чому не можна брати адресу елемента мапи?
11. Чому `map` в Go не є потокобезпечною «з коробки»?
12. Чи може структура бути ключем у `map` і які обмеження на це накладаються?
    Чим це краще за вкладені map?
13. Як порівнювати дві структури — коли це компілюється, а коли ні?
14. Як реалізувати порівняння двох структур, якщо вони містять слайси або мапи?
    Що таке `reflect.DeepEqual()`?
15. Що відбувається при приведенні між іменованими типами з однаковою
    структурою, якщо в них різні методи?
16. Що таке `Memory Alignment` (вирівнювання) і як воно впливає на розмір
    структур?
17. Чому передача великої структури «за значенням» часто працює повільніше, ніж
    передача вказівника?
18. Чому `map` повільніший за `slice` при sequential access і коли обрати що?

---

## 3. Інтерфейси та типова система

1. Як перевірити, чи змінна реалізує інтерфейс?
2. Що таке `type assertion` та `type switch` — яка від них користь і як обробити
   assertion без паніки?
3. Чому `interface{}` і `any` ідентичні, але `*interface{}` майже завжди
   помилка?
4. Коли варто використовувати `interface{}` (`any`), а коли це вважається
   поганим тоном?
5. У чому перевага прийому інтерфейсів та повернення конкретних структур?
6. Навіщо в Go використовують інтерфейси з одним методом (наприклад `io.Reader`,
   `fmt.Stringer`) і яку архітектурну користь це дає?
7. Чому `nil != nil` в Go і як це пов'язано з інтерфейсами?
8. Чи можна викликати методи на `nil`-значеннях і де це активно
   використовується?

---

## 4. Конкурентність — горутини та канали

1. Як вказати головній горутині очікувати завершення роботи всіх робочих
   горутин?
2. Чому в циклах використовували шаблон `value := value` і чи він актуальний
   після Go 1.22?
3. Чи може використання горутин сповільнити систему і в яких випадках?
4. У чому різниця між буферизованими та небуферизованими каналами? Коли замість
   каналів доречно використовувати slice + mutex?
5. Що станеться при читанні, записі або закритті `nil`-каналу?
6. Як і навіщо використовувати `nil`-канали в `select`? Чому `nil`-канал блокує
   назавжди і як це використати?
7. У яких випадках доцільно використовувати `select` із гілкою `default` і які
   сценарії це покриває?
8. Як працює `select` при одночасному надходженні даних у кілька каналів?
9. Як безпечно закрити канал у Go, якщо в нього пишуть кілька горутин?
10. Як реалізувати семафор через буферизований канал?
11. Як реалізувати патерни `Fan-in` та `Fan-out`?
12. Чому не варто використовувати канали для передачі великих обсягів даних?
13. Як коректно повернути помилку з горутини в основний потік?
14. Чи може `defer` у Go перехопити (`recover`) паніку, яка сталася в дочірній
    горутині?
15. Розкажіть про конкурентні патерни в Go.

---

## 5. Конкурентність — примітиви синхронізації

1. Коли використовувати `sync.Mutex`, а коли `sync.RWMutex`?
2. Чому не можна копіювати об'єкти `sync.Mutex`?
3. Чому читання і запис shared state без синхронізації — це data race, навіть
   якщо «логічно безпечно»?
4. Що таке Race Condition і як працює детектор `-race`? Що він може і чого не
   може виявити?
5. У чому переваги atomic-операцій порівняно з mutex для простих конкурентних
   операцій?
6. Як працює `sync.WaitGroup` і що буде при від'ємному лічильнику? Чому не можна
   викликати `wg.Done()` до `wg.Add()`?
7. У чому різниця між `sync.WaitGroup` і `errgroup.Group`? Коли використовувати
   кожен?
8. Опишіть призначення та реалізацію `sync.Once` — як він гарантує одноразову
   ініціалізацію?
9. Що таке `sync.Cond` і коли він переважає канал?
10. Як влаштований `sync.Map`, коли він дає кращу продуктивність порівняно з
    map + mutex і де він використовується в стандартній бібліотеці?
11. Що таке конкурентні тести в Go і навіщо їх використовують?

---

## 6. Context

1. Навіщо в Go використовується `context.Context` і як він передається через
   дерево викликів функцій?
2. Чи є `context.Context` незмінним (immutable) і що це означає на практиці?
3. Як використання `context.WithCancel` допомагає уникнути витоків горутин?
4. Чому в Go для `context.WithValue` використовують нестандартні типи ключів
   (наприклад, `struct{}`) і як це запобігає колізіям?
5. У чому різниця між `context.Value` та передачею параметрів через аргументи
   функції?

---

## 7. Управління пам'яттю та продуктивність

1. Як працює алокація на Stack vs Heap у Go?
2. Як мінімізувати алокації в купі за допомогою `sync.Pool`?
3. Що означають змінні середовища `GOGC` і `GOMEMLIMIT` і як вони впливають на
   роботу garbage collector?
4. Що таке `runtime.SetFinalizer` і чи використовується він у стандартній
   бібліотеці?
5. Як знайти витік пам'яті за допомогою `pprof`?
6. Як знайти hot paths і виміряти throughput?
7. Як оптимізувати роботу з рядками за допомогою `strings.Builder`? Чому не
   можна конкатенувати в циклі?
8. Як оптимізувати серіалізацію?
9. Як оптимізувати роботу з файлами?
10. Як працює batching і коли він доречний?
11. Коли code generation (`go generate`) краще ніж reflection?
12. Що таке Escape Analysis і як його перевірити прапорцями компілятора?

---

## 8. Обробка помилок

1. Чому `panic` і `recover` — не заміна нормальному error handling?
2. Як у Go працюють `errors.Is` і `errors.As` при error wrapping і в чому між
   ними різниця?
3. Коли варто використовувати custom error type замість sentinel error і які
   практичні наслідки цього вибору для архітектури?

---

## 9. Ініціалізація, defer та lifecycle

1. Як поводиться `defer` усередині циклу і які можуть бути наслідки для пам'яті
   та виконання?
2. Як працює функція `init` і чи можна покладатися на порядок її виконання?
3. Чому варто уникати глобальних змінних та `init`-функцій у бібліотеках?

---

## 10. JSON та серіалізація

1. Що буде, якщо серіалізувати в JSON структуру з полями, що починаються з малої
   літери?
2. Які є способи діставати дані з JSON у Go?
3. Яка різниця між `json.Marshal` і `json.Encoder`?
4. Що таке `json.RawMessage` і коли воно корисне?
5. Як реалізувати кастомний маршалер для JSON?
6. Як парсити різнотипний JSON, якщо вхідні дані або якісь з полів можуть бути
   або масивом `[...]`, або об'єктом `{...}`?
7. Як у Go тестувати серіалізацію (XML/JSON), коли порядок ключів у мапі не
   детермінований?
8. У чому переваги та недоліки Protobuf порівняно з JSON? Як відрізняється
   серіалізація в Protobuf?

---

## 11. HTTP та мережа

1. Чому `http.Client` варто перевикористовувати замість створювати новий на
   кожен запит?
2. Чому потрібно обов'язково закривати `resp.Body` після HTTP-запиту?
3. Як `http.DefaultServeMux` відрізняється від кастомного `ServeMux`?
4. Як у Go правильно реалізувати graceful shutdown HTTP-сервера і фонового
   воркера?

---

## 12. Стандартна бібліотека — різне

1. Чому `time.Time` порівнювати через `.Equal()`, а не `==`?

---

## 13. Бази даних

1. Як працюють індекси? Як обирати індекси для таблиць?
2. Що таке Materialized View і чим він відрізняється від звичайного View?
3. Що таке ACID? Прокоментуйте, як ACID реалізований у PostgreSQL.
4. Яка різниця між BASE та ACID?
5. Назвіть рівні ізоляції транзакцій.
6. Для чого потрібні графові бази даних?
7. Якщо дані прив'язані до часу, які бази даних варто використовувати?
8. Як працює master-slave реплікація?
9. Що таке шардинг і які його види?
10. Розкажіть про ваш досвід оптимізації бази даних. Які інструменти
    використовували?
11. Як `pgx` відрізняється від `lib/pq` щодо продуктивності та функціональності?

---

## 14. Тестування

1. Як писати unit-тести в Go?
2. Яка різниця між `t.Error` і `t.Fatal` у тестах?
3. Як `testify/assert` відрізняється від `testify/require` семантично?
4. Як `t.Run` дозволяє запускати підтести і фільтрувати їх?
5. Як тестувати HTTP-handlers?
6. Як тестувати помилки?
7. Як мокати зовнішні залежності без використання сторонніх фреймворків?
8. Як використовувати `TestMain` для налаштування тестового оточення?
9. Як використовувати golden files?
10. Як правильно тестувати Go-код, що використовує `time.Now()`, щоб тести були
    детермінованими?
11. Як `t.Parallel()` прискорює набір тестів і де він може їх зламати?
12. Як виміряти code coverage?
13. Що таке benchmarking і як його запускати? Як `testing.B` реалізує benchmark
    і що скидає `b.ResetTimer`?
14. Як запускати бенчмарки з контролем часу та кількості ітерацій?
15. Як інструмент `benchstat` порівнює два набори результатів бенчмарків і як
    визначає значущість змін?
16. Що таке fuzz testing?
17. Які є способи запуску тестів із БД у CI (Testcontainers, docker-compose,
    GitHub Actions services)? Які переваги кожного підходу?

---

## 15. Інструменти та збірка

1. Що таке `go vet`?
2. Як профілювати Go-додаток (`pprof`)?
3. Як працює `go build` і крос-компіляція?
4. Як контейнеризувати Go-додаток у Docker?
5. Як зменшити розмір Docker-образу для Go-застосунку (multi-stage build)?

---

## 16. Моніторинг та спостережуваність

1. Які інструменти зазвичай використовують для збору метрик і логів? Як працює
   Prometheus?

---

## 17. Архітектура та мікросервіси

1. Яка різниця між мікросервісами та монолітом? Які переваги та недоліки?
2. Як реалізувати аутентифікацію в мікросервісній архітектурі?
3. Як спроєктувати та реалізувати API Gateway у мікросервісній архітектурі і які
   задачі він має вирішувати?
4. Як працює service discovery у мікросервісній архітектурі і як сервіси
   знаходять один одного без статичних IP/хостів?
5. Як у мікросервісній архітектурі уникнути tight coupling, щоб система
   залишалась масштабованою і легко змінюваною?
6. Як організувати backward compatibility?
7. Як у Go-застосунках реалізують оновлення конфігурації «на льоту» без
   перезапуску сервісу?
8. Як у Go-сервісі реалізувати throttling (обмеження частоти запитів) для
   захисту системи від перевантаження?
9. Як у Go працює caching layer і яку проблему вирішує `singleflight.Group` при
   конкурентних запитах?
10. Як у Go-сервісі спроєктувати multi-tenant архітектуру з ізоляцією даних між
    клієнтами?
11. Як правильно організувати обробку довготривалих задач (long-running tasks),
    щоб не блокувати HTTP-запити?
12. Як побудувати міжсервісну транзакцію?
13. Які проблеми вирішує патерн Saga?
14. Що таке Event Sourcing?
15. Сформулюйте CAP-теорему.
16. Розкажіть про алгоритм консенсусу Raft.

---

## 18. DevOps та CI/CD

1. Як організувати CI/CD для Go?
2. Як організувати rollback у CI/CD для Go-сервісу, щоб швидко і безпечно
   відкотити невдалий реліз?
3. Як працює blue-green deployment?
