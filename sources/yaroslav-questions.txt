## Основні практичні запитання

1. Чому в циклах використовували шаблон `value := value` і чи він актуальний після Go 1.22?
2. Який порядок ітерації ключів у map і чи можна на нього покладатися?
3. Як невизначений порядок map впливає на тести та серіалізацію?
4. Чи має `select` гарантований порядок виконання кейсів?
5. Як і навіщо використовувати `nil`-канали в `select`?
6. Чи може використання горутин сповільнити систему і в яких випадках?
7. У чому різниця між `sync.WaitGroup` і `errgroup.Group`?
8. Коли краще використовувати `errgroup` замість `WaitGroup`?
9. Чи може структура бути ключем у map і які обмеження цього підходу?
10. Які переваги використання структур як ключів у map?

## Робота з runtime і memory

11. Що таке `runtime.SetFinalizer` і де його реально використовують?

## Тестування та продуктивність

12. Як запускати бенчмарки в Go з контролем часу та кількості ітерацій?

## JSON і робота з даними

13. Як парсити JSON, якщо структура може бути або масивом `[]`, або об’єктом `{}`?
14. Як обробляти динамічні (різнотипні) JSON-структури з інших мов, наприклад PHP?

## Інтеграційне тестування і CI

15. Які є способи запуску тестів із БД у CI (testcontainers, docker-compose, GitHub Actions services)?
16. Які переваги та недоліки `testcontainers` порівняно з `docker-compose`?
17. Коли краще використовувати GitHub Actions services для тестів із БД?
18. Як обрати підхід для тестів із БД залежно від проєкту?
