# Golang Interview Questions

### Selected Unordered Golang Interview Questions
Чому в циклах використовували шаблон `value := value` і чи він актуальний після Go 1.22?
Який порядок ітерації ключів у map і чи можна на нього покладатися? Як впливає на тести та серіалізацію?
Як і навіщо використовувати `nil`-канали в `select`?
Чи може використання горутин сповільнити систему і в яких випадках?
У чому різниця між `sync.WaitGroup` і `errgroup.Group`? Коли використовувати кожен з них?
Чи може структура бути ключем у map і які обмеження на це накладаються? Чим це краще за вкладені map?
Чому nil != nil?
Чи можна викликати методи на nil-значеннях, де це активно використовується?
Що таке `runtime.SetFinalizer` і чи використується у стандартній бібліотеці?
Як запускати бенчмарки в Go з контролем часу та кількості ітерацій?
Як парсити JSON, якщо структура може бути або масивом `[...]`, або об’єктом `{...}`?
Як обробляти динамічні (різнотипні) JSON-структури з інших мов, наприклад PHP?
Які є способи запуску тестів із БД у CI (testcontainers, docker-compose, GitHub Actions services)? Які плюси кожного підходу?

Як працює внутрішня структура `slice` і що відбувається при передачі його у функцію?
Чому `map` в Go не є потокобезпечною "з коробки"?
Навіщо використовувати `struct{}` (порожню структуру) і в яких сценаріях це ефективно?
Як реалізувати порівняння двох структур, якщо вони містять слайси або мапи?
Що таке `shadowing` змінних і як він може призвести до помилок у бізнес-логіці?
Що таке `type assertion` та `type switch`, яка від них користь?
Чи гарантується, що вказівник (адреса) на елемент слайсу в Go залишиться валідним після виклику append?
Що відбувається з приведенням між іменованими типами з однаковою структурою, якщо в них різні методи або різні сигнатури методів?
Як працює `init` функція і чи можна покладатися на порядок її виконання?
У чому перевага прийому інтерфейсів та повернення конкретних структур?
Коли варто використовувати `interface{}` (any), а коли це вважається поганим тоном?
У чому різниця між буферизованими та небуферизованими каналами в Go? Коли замість каналів доречно використовувати slice + mutex, і чи бувають випадки, коли достатньо лише slice?
Що станеться при читанні, записі або закритті `nil` каналу?
Як безпечно закрити канал у Go, якщо в нього пишуть кілька горутин?
У яких випадках у Go доцільно використовувати select з default гілкою, і які сценарії це покриває?
Навіщо в Go використовується context.Context і як він передається (протікає) через дерево викликів функцій?
Чи є context.Context в Go незмінним (immutable), і що це означає на практиці?
Чому в Go для context.WithValue використовують різні типи ключів (наприклад, string або struct{}), і як це впливає на ризик колізій?
Як використання context.WithCancel допомагає уникнути витоків горутин у Go?
Різниця між `context.Value` та передачею параметрів через аргументи функції.
Як працює `select` при одночасному надходженні даних у кілька каналів?
Як реалізувати патерн `Fan-in` та `Fan-out`?
Чому не варто використовувати канали для передачі великих обсягів даних?
Як працює `Stack` vs `Heap` алокація в Go?
Як мінімізувати алокації в купі за допомогою `sync.Pool`?
Що означають змінні середовища GOGC і GOMEMLIMIT у Go, і як вони впливають на роботу garbage collector?
Як знайти витік пам'яті за допомогою `pprof`?
Коли використовувати `sync.Mutex`, а коли `sync.RWMutex`?
Що таке `Race Condition` і як працює `-race` детектор?
Що таке конкурентні тести в Go і навіщо їх використовують?
Чому не можна копіювати об'єкти `sync.Mutex`?
Як працює `sync.WaitGroup` і що буде при від'ємному лічильнику?
Опишіть призначення `sync.Once` та його реалізацію.
У чому переваги використання atomic порівняно з mutex для простих конкурентних операцій у Go?
Що таке `Memory Alignment` (вирівнювання) і як воно впливає на розмір структур?
Як у Go працюють errors.Is і errors.As при error wrapping, і в чому між ними різниця?
Як використовувати `TestMain` для налаштування тестового оточення?
Як мокати зовнішні залежності без використання сторонніх фреймворків?
Чому варто уникати глобальних змінних та `init` функцій у бібліотеках?
Як зменшити розмір Docker-образу для Go-застосунку (multi-stage build)?
Чому Go добре підходить для написання Cloud Native сервісів?
Чи може defer у Go перехопити (recover) паніку, яка сталася в дочірній goroutine?
Чому передача великої структури в Go “за значенням” часто працює повільніше, ніж передача вказівника на неї?
Як влаштований sync.Map у Go, коли він дає кращу продуктивність порівняно з map + mutex, і в яких місцях стандартної бібліотеки Go він використовується?
Як реалізувати кастомний маршалер для JSON?
Чому в Go (і загалом при роботі з net/http) потрібно обов’язково закривати resp.Body після HTTP-запиту?
Як працює серіалізація в `Protobuf` порівняно з JSON у Go?
Як оптимізувати роботу з рядками за допомогою `strings.Builder`? Чому не можна просто конкатувати в циклі?
Як обробити type assertion без паніки?
Як поводиться defer у Go, якщо його виклик знаходиться всередині циклу, і які можуть бути наслідки для пам’яті та виконання?
Як тестувати HTTP handlers?
Як тестувати помилки?
Як використовувати golden files?
Як у Go тестувати серіалізацію (XML/JSON), коли порядок атрибутів у мапі не детермінований і тому вихідний XML/JSON може відрізнятися між запусками?
Як знайти hot paths?
Як оптимізувати serialization?
Як працює batching?
Як виміряти throughput?
Як оптимізувати роботу з файлами?
Як у Go правильно реалізувати graceful shutdown HTTP сервера і фонового воркера, щоб коректно завершити запити, goroutines і ресурси?
Як організувати backward compatibility?
Як у Go застосунках реалізують оновлення конфігурації “на льоту” без перезапуску сервісу?
Як у Go-сервісі спроєктувати та реалізувати multi-tenant архітектуру, щоб одна система обслуговувала кілька клієнтів із ізоляцією даних?
Як у Go працює caching layer, і яку проблему вирішує singleflight.Group при кешуванні та конкурентних запитах?
Як у Go або бекенд-системі правильно організувати обробку довготривалих задач (long-running tasks), щоб не блокувати HTTP-запити, забезпечити надійність і масштабування?
Як у Go-сервісі реалізувати throttling (обмеження частоти запитів), щоб захистити систему від перевантаження і контролювати rate of requests?
Як працює blue-green deployment?
Як спроєктувати та реалізувати API Gateway у мікросервісній архітектурі (зокрема в Go), і які задачі він має вирішувати?
Як працює service discovery у мікросервісній архітектурі і як сервіси знаходять один одного без статичних IP/хостів?
Як у мікросервісній або Go-архітектурі уникнути tight coupling (сильної залежності між компонентами), щоб система залишалась масштабованою і легко змінюваною?
Як організувати CI/CD для Go?
Як організувати rollback у CI/CD для Go-сервісу, щоб швидко і безпечно відкотити невдалий реліз у production?
Як slice expression `a[low:high:max]` контролює cap нового слайса?
Чому не можна брати адресу елемента мапи?
Як у Go ефективно видаляти елементи зі слайса без збереження порядку, щоб отримати максимальну продуктивність?
Як ітерувати `map` у передбачуваному порядку?
Чому `interface{}` і `any` ідентичні, але `*interface{}` майже завжди помилка?
Навіщо в Go використовують інтерфейси з одним методом (наприклад io.Reader, fmt.Stringer), і яку архітектурну користь це дає?
Як порівнювати дві struct — коли це компільується, а коли ні?
Як реалізувати semaphore через buffered channel?
Чому `nil` channel у `select` блокує навічно і як це використати?
Що таке `sync.Once` і як він гарантує одноразову ініціалізацію?
Чому в Go не можна викликати wg.Done() у горутині до wg.Add() у sync.WaitGroup, навіть якщо іноді такі патерни зустрічаються в прикладах, і чому це небезпечно в реальному коді?
Що таке `sync.Cond` і коли він переважає channel?
Чому читання і запис shared state без синхронізації — data race навіть якщо "логічно безпечно"?
Як `-race` detector виявляє data races і що він не може виявити?
Чому `make([]T, 0, n)` краще ніж `var s []T` у відомих розмірах?
Коли в Go варто використовувати custom error type замість sentinel error, і які практичні наслідки цього вибору для архітектури та обробки помилок?
Чому `panic` і `recover` — не заміна нормального error handling?
Як коректно повернути помилку з горутини в основний потік?
Як у Go правильно передати помилку, яка виникла в горутині, назад у головну горутину (main / викликаючий потік)?